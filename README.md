PanelBeatz - Static panel-beating website

Open `index.html` in your browser to view the site locally. You can also serve the folder using a simple static server for better behavior:

Using Python 3:

```powershell
python -m http.server 8000
```

Then open http://localhost:8000 in your browser.
